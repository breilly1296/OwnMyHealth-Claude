# Frontend environment — staging build.
# Vite picks this up when invoked with `--mode staging` (see deploy-staging.yml).
# VITE_* variables are embedded at build time; never put secrets here.

VITE_API_URL=https://api-staging.ownmyhealth.io/api/v1
